{% extends 'base.html.twig' %}

{% block body %}
    <h1>Product creation</h1>
<div class="well">
    {{ form_start(form) }}
    <div class="form-group">
        {# Génération du label. #}
        {{ form_label(form.name, "Nom du produit", {'label_attr': {'class': 'col-sm-2 control-label'}}) }}

        {# Affichage des erreurs pour ce champ précis. #}
        {{ form_errors(form.name) }}

        <div class="col-sm-10">
            {# Génération de l'input. #}
            {{ form_widget(form.name, {'attr': {'class': 'form-control'}}) }}
        </div>
    </div>

    <div class="form-group">
        {# Génération du label. #}
        {{ form_label(form.quantity, "Quantity du produit", {'label_attr': {'class': 'col-sm-2 control-label'}}) }}

        {# Affichage des erreurs pour ce champ précis. #}
        {{ form_errors(form.quantity) }}

        <div class="col-sm-10">
            {# Génération de l'input. #}
            {{ form_widget(form.quantity, {'attr': {'class': 'form-control'}}) }}
        </div>
    </div>
    <div class="form-group">
        {# Génération du label. #}
        {{ form_label(form.delivery, "Tu peux livrer ", {'label_attr': {'class': 'col-sm-2 control-label'}}) }}

        {# Affichage des erreurs pour ce champ précis. #}
        {{ form_errors(form.delivery) }}

        <div class="col-sm-10">
            {# Génération de l'input. #}
            {{ form_widget(form.delivery, {'attr': {'class': 'form-control'}}) }}
        </div>
    </div>
    <div class="form-group">
        {# Génération du label. #}
        {{ form_label(form.category, "Category: ", {'label_attr': {'class': 'col-sm-2 control-label'}}) }}

        {# Affichage des erreurs pour ce champ précis. #}
        {{ form_errors(form.category) }}

        <div class="col-sm-10">
            {# Génération de l'input. #}
            {{ form_widget(form.category, {'attr': {'class': 'form-control'}}) }}
        </div>
    </div>
    <div class="form-group">
        {# Génération du label. #}
        {{ form_label(form.description, "Description: ", {'label_attr': {'class': 'col-sm-2 control-label'}}) }}

        {# Affichage des erreurs pour ce champ précis. #}
        {{ form_errors(form.description) }}

        <div class="col-sm-10">
            {# Génération de l'input. #}
            {{ form_widget(form.description, {'attr': {'class': 'form-control'}}) }}
        </div>
    </div>
    <div class="form-group">
        {# Génération du label. #}
        {{ form_label(form.adress, "où disponible le produit(adresse) ", {'label_attr': {'class': 'col-sm-2 control-label'}}) }}

        {# Affichage des erreurs pour ce champ précis. #}
        {{ form_errors(form.adress) }}

        <div class="col-sm-10">
            {# Génération de l'input. #}
            {{ form_widget(form.adress, {'attr': {'class': 'form-control'}}) }}
        </div>
    </div>
    <div class="form-group">
        {# Génération du label. #}
        {{ form_label(form.price, "le prix ", {'label_attr': {'class': 'col-sm-2 control-label'}}) }}

        {# Affichage des erreurs pour ce champ précis. #}
        {{ form_errors(form.price) }}

        <div class="col-sm-10">
            {# Génération de l'input. #}
            {{ form_widget(form.price, {'attr': {'class': 'form-control'}}) }}
        </div>
    </div>
    <div class="form-group">
        {# Génération du label. #}
        {{ form_label(form.tel, "N° tel ", {'label_attr': {'class': 'col-sm-2 control-label'}}) }}

        {# Affichage des erreurs pour ce champ précis. #}
        {{ form_errors(form.tel) }}

        <div class="col-sm-10">
            {# Génération de l'input. #}
            {{ form_widget(form.tel, {'attr': {'class': 'form-control'}}) }}
        </div>
    </div>
        <input type="submit" value="Next step" class="btn btn-success" />
    {{ form_end(form) }}
        <br>
            <a href="{{ path('product_list') }}" class="btn btn-primary">Back to the list</a>
</div>
{% endblock %}
