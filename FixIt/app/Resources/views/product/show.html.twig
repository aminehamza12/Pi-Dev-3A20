{% extends 'base.html.twig' %}

{% block body %}



    <!--
    <div class="row">
        <div class="col-12 col-lg-7">
            <div class="single_product_thumb">
                <div id="product_details_slider" class="carousel slide" data-ride="carousel">
                    <ol class="carousel-indicators">
                        {% for m in media %}
                        <li class="" data-target="#product_details_slider" data-slide-to="0" style="background-image: url({{ asset(m.brochure) }});">
                        </li>
                        {% endfor %}
                    </ol>
                    <div class="carousel-inner">
                        {% for m in media %}
                        <div class="carousel-item">
                            <a class="gallery_img" href="#">
                                <img class="d-block w-100" src="{{ asset(m.brochure) }}" alt="First slide">
                            </a>
                        </div>
                        {% endfor %}
                    </div>
                </div>
            </div>
        </div>
        <div class="col-12 col-lg-5">
            <div class="single_product_desc">

                <div class="product-meta-data">
                    <div class="line"></div>
                    <p class="product-price">{{ product.price }}</p>
                    <a href="#">
                        <h6>{{ product.name }}</h6>
                    </a>

                    <div class="ratings-review mb-15 d-flex align-items-center justify-content-between">
                        <div class="ratings">
                            <i class="fa fa-star" aria-hidden="true"></i>
                            <i class="fa fa-star" aria-hidden="true"></i>
                            <i class="fa fa-star" aria-hidden="true"></i>
                            <i class="fa fa-star" aria-hidden="true"></i>
                            <i class="fa fa-star" aria-hidden="true"></i>
                        </div>
                        <div class="review">
                            <a href="#">Write A Review</a>
                        </div>
                    </div>

                    <p class="avaibility"><i class="fa fa-circle"></i> In Stock</p>
                </div>
                <div class="short_overview my-5">
                    <p></p>
                </div>

                <form class="cart clearfix" method="post">
                    <div class="cart-btn d-flex mb-50">
                        <p>Qty</p>
                        <div class="quantity">
                            <span class="qty-minus" onclick="var effect = document.getElementById('qty'); var qty = effect.value; if( !isNaN( qty ) &amp;&amp; qty > 1 ) effect.value--;return false;"><i class="fa fa-caret-down" aria-hidden="true"></i></span>
                            <p>{{ product.quantity }}</p>
                            <span class="qty-plus" onclick="var effect = document.getElementById('qty'); var qty = effect.value; if( !isNaN( qty )) effect.value++;return false;"><i class="fa fa-caret-up" aria-hidden="true"></i></span>

                            {% if product.delivery %}Yes{% else %}No{% endif %}
                        </div>
                    </div>

                    <ul>
                        <li>
                            <a href="{{ path('product_index') }}">Back to the list</a>
                        </li>
                        <li>
                            <a href="{{ path('product_edit', { 'id': product.id }) }}">Edit</a>
                        </li>
                        <li>
                            <a href="{{ path('product_delete', { 'id': product.id }) }}">supprimer</a>
                            {{ form_start(delete_form) }}
                            <input type="submit" value="Delete">
                            {{ form_end(delete_form) }}
                        </li>
                    </ul>
                    <button type="submit" name="addtocart" value="5" class="btn amado-btn">Add to cart</button>
                </form>
            </div>
        </div>
    </div> -->

    <section class="sec-product-detail bg0 p-t-65 p-b-60"   >
        <div class="container">

            <div class="row">
                <div class="col-md-6 col-lg-7 p-b-30">
                    <div class="p-l-25 p-r-30 p-lr-0-lg">
                        <div class="wrap-slick3 flex-sb flex-w">
                            <div class="wrap-slick3-dots"></div>
                            <div class="wrap-slick3-arrows flex-sb-m flex-w"></div>

                            <div class="slick3 gallery-lb">
                                <div class="item-slick3" data-thumb="{{ media[0].brochure  }}">
                                    <div >
                                        <center><img height="500px" width="500px" src="{{ media[0].brochure  }}"  alt="IMG-PRODUCT"></center>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <fieldset>
                <div class="col-md-6 col-lg-5 p-b-30">
                    <div class="p-r-50 p-t-5 p-lr-0-lg" style="text-align: center">
                        <br><br><br><br><br>
                        <h4 class="mtext-105 cl2 js-name-detail p-b-14">
                            {{ product.name }}
                        </h4>
                        <details>
                            <summary>pour plus de détails  → :</summary>
                            <p> - by : {{ product.user.nom }}</p>
                            <p> {{ product.description }}</p>
                        </details>


                        <span class="mtext-106 cl2">
                            <h5 style="color: #06ff00">{{ product.price }} TND</h5>
						</span>
                        <h4 class="mtext-105 cl2 js-name-detail p-b-14">
                            Product owner: {{ product.user.nom }} {{ product.user.prenom }}
                        </h4>


                        <!--  -->

                            <div class="flex-w flex-r-m p-b-10">
                                <div class="size-204 flex-w flex-m respon6-next">
                                    <div class="wrap-num-product flex-w m-r-20 m-tb-10">
                                        <div class="btn-num-product-down cl8 hov-btn3 trans-04 flex-c-m">
                                            <i class="fs-16 zmdi zmdi-minus"></i>
                                        </div>

                                       <h6>Quantité disponible :</h6> <input class="mtext-104 cl3 txt-center num-product" type="text" name="num-product" value="{{ product.quantity }}" disabled>
                                        <div>
                                            <i class="glyphicon glyphicon-eye-open">{{ product.view }}</i>
                                        </div>
                                        <div>
                                            <br>
                                            <h6>Nombre des commandes passer:</h6>
                                            <i class=" 	glyphicon glyphicon-list-alt">{{ product.nbcommande }}</i>
                                        </div>
                                        <div class="btn-num-product-up cl8 hov-btn3 trans-04 flex-c-m">
                                            <i class="fs-16 zmdi zmdi-plus"></i>
                                        </div>
                                    </div>
                                    {% if user.id == product.user.id %}
                                        <div class="list-group">
                                            <br>
                                            <a href="{{ path('product_edit', { 'id': product.id }) }}" class="btn btn-info list-group-item" style="color: deepskyblue">edit</a>

                                            <a href="{{ path('product_delete', { 'id': product.id})}}" class="btn btn-danger  list-group-item" style="color: #ff0003">delete</a>
                                        </div>
                                    {% elseif product.quantity > 0 %}

                                            <a href="{{ path('commande_new', { 'id': product.id})}}" class="btn btn-success">reserver</a>

                                    {% endif %}

                                </div>
                            </div>
                        </div>

                        <!--  -->

                    </div>
                </fieldset>
                </div>
            </div>
        </div>

    </section>


{% endblock %}
