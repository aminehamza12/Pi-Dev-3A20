{% extends 'adminbase.html.twig' %}

{% block body %}
    <h1>Categorieifixit</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorieifixit.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ categorieifixit.type }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('categorieifixit_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('categorieifixit_edit', { 'id': categorieifixit.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
